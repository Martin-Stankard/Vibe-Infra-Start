version: '3.8'

services:
  mongodb:
    image: mongo:latest
    container_name: my-mongodb-server
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_ROOT_USERNAME: admin
      MONGO_INITDB_ROOT_PASSWORD: password
    volumes:
      - mongo_data:/data/db
    restart: unless-stopped

  mongo-client:
    image: mongo-express:latest # Or any other MongoDB client image
    container_name: my-mongodb-client
    ports:
      - "8081:8081" # Example for mongo-express, adjust for other clients
    environment:
      ME_CONFIG_MONGODB_SERVER: mongodb # Connect to the 'mongodb' service
      ME_CONFIG_MONGODB_ADMINUSERNAME: admin
      ME_CONFIG_MONGODB_ADMINPASSWORD: password
    depends_on:
      - mongodb # Ensure MongoDB starts before the client
    restart: unless-stopped

volumes:
  mongo_data:
    driver: local