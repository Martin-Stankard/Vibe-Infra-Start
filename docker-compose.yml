
services:
    mongodb:
        image: mongo:6
        container_name: THIS-mongodb
        environment:
          ALLOW_EMPTY_PASSWORD  : "yes"
          MONGODB_INITDB_DATABASE: admin
          
        ports:
          - "27017:27017"

        volumes:
          - ../test-zero:/data/db #immediate goal is save here, my local folder nextdoor, from mongo-express and node-stream

    mongo-express:
        image: mongo-express
        container_name: THIS-mongo-express
        environment:
            ME_CONFIG_MONGODB_SERVER: mongodb
            ME_CONFIG_MONGODB_PORT: 27017
            ME_CONFIG_BASICAUTH_USERNAME: "" # Set username to empty string to disable basic auth
            ME_CONFIG_BASICAUTH_PASSWORD: "" # Set password to empty string to disable basic auth
        depends_on:
            - mongodb
        ports:
          - "8081:8081"


    # open-webui:
    #   image: ghcr.io/open-webui/open-webui:latest
    #   container_name: open-webui
    #   volumes:
    #   - open-webui:/app/backend/data
    #   pull_policy: always
    #   depends_on:
    #   - ollama
    #   ports:
    #   - 8080:8080
    #   environment: 
    #   - OLLAMA_BASE_URLS=http://host.docker.internal:11434
    #   - WEBUI_AUTH=False
    #   - WEBUI_SECRET_KEY=123456
    #   networks:
    #   - ollama-docker   


    # ollama:
    #   image: ollama/ollama:latest
    #   container_name: ollama
    #   ports:
    #   - 11434:11434
    #   volumes:
    #   - ollama:/root/.ollama
    #   pull_policy: always
    #   tty: true
    #   restart: always
    #   environment:
    #   - OLLAMA_KEEP_ALIVE=24h
    #   networks:
    #   - ollama-docker
    #   deploy:
    #     resources:
    #       reservations:
    #         devices:
    #           - driver: nvidia
    #             count: 1
    #             capabilities: [gpu]

    node-stream:
        build: ./node-stream
        ports:
          - "8079:8079"
        environment:
          MONGO_URL: mongodb://admin:password@mongo:27017/admin
        depends_on:
          - mongodb

networks:
  ollama-docker:
    driver: bridge  

volumes:
  ollama:
  open-webui:
  